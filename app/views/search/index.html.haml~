%h2 Search Results
%div.row
  %table.col-md-4.col-md-offset-4
    %thead
      %tr
        %th First Name
        %th Last Name
    %tbody
      - if(@display != nil)
        - @display.each do |user|
          %tr
            %td= link_to user.firstName, user_path(user.id, :searchfor => True)
            %td= user.lastName

%br
#{link_to "Back to Profile!", '/users/' + session[:id]}
